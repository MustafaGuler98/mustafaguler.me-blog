# YAML mime
AddType text/yaml .yml
AddType text/yaml .yaml

# (Varsa) BasicAuth kuralın burada olabilir, ama oauth klasörünü dışarıda bırak:
# Örn:
# <FilesMatch "^(?!oauth/).*$">
#   AuthType Basic
#   AuthName "Restricted"
#   AuthUserFile /home/.../public_html/blog/admin/.htpasswd
#   Require valid-user
# </FilesMatch>

# OAuth klasörü açık kalsın
<Directory "/home/USERNAME/public_html/blog/admin/oauth">
  Require all granted
</Directory>

# (İsteğe bağlı) cache patlatma
<Files "config.yml">
  Header set Cache-Control "no-cache, no-store, must-revalidate"
</Files>
