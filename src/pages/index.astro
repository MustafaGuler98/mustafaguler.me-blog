---
const BASE = import.meta.env.BASE_URL;                
const trPath = `${BASE}/tr`;     
---

<!doctype html>
<html lang="tr">
  <head>
    <meta charset="utf-8" />
    <title>Yönlendiriliyor…</title>
    <meta http-equiv="refresh" content={`0; url=${trPath}`} />
  </head>
  <body>
    <p>
      Yönlendiriliyor… Eğer otomatik gitmezse
      <a href={trPath}>TR</a>.
    </p>
  </body>
</html>
